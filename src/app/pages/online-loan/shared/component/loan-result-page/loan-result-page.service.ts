/**
 *線上申貸 結果頁(共用)
 */
import { Injectable } from "@angular/core";
import { Logger } from '@core/system/logger/logger.service';

@Injectable()

export class LoanResultPageService {

    constructor(
        private _logger: Logger,
    ) { }
}